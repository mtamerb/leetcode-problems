class Solution {
    public int countOdds(int low, int high) {

        int countOfOdd = (high + 1) / 2 - (low / 2);

        return countOfOdd;
    }
}
// for even  int countOfEven = (high / 2) - ((low - 1) / 2);